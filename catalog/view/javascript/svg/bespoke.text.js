var bespokeText = function(drawObject, options) {


  var vars = {
  }

  this.construct = function(drawObject, options){
     this.drawSVG = drawObject  //don't create a newone - just use the original
     $.extend(vars , options)

  };

  this.drawSVG

  var root = this






this.construct(drawObject, options)
}
